info:
  name: Get Movie By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/movies/:id"
  params:
    - name: id
      value: "11521151"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
