info:
  name: Get All Movies
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/movies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
