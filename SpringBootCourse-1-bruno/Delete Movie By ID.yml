info:
  name: Delete Movie By ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/movies/:id"
  params:
    - name: id
      value: "2666"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
